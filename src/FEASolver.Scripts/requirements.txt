# Runtime dependencies for FEASolver.Scripts.
# Install via: pip install -r requirements.txt
# For pythonOCC (optional, STEP repair): conda install -c conda-forge pythonocc-core=7.7.2
#
# Versions are pinned to the validated set (the suite is green on these exact
# versions — see ship_readiness.md). CI installs this same file, so dev and CI
# run identical dependencies. Minimum interpreter: Python 3.11.
#
# audit-006: previously numpy was pinned to 1.26.4 while the dev/test environment
# ran numpy 2.x — CI and dev resolved different numpy majors. Now reconciled.
gmsh==4.15.2
numpy==2.2.6
pytest==9.0.3
